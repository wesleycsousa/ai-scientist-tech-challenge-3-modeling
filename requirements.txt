# Manipulação e análise de dados
# (sem pin de versão exata: Python 3.14 é muito recente e versões antigas
# do pandas/numpy não têm wheel pré-compilado pra ele, o que obrigava a
# compilar do zero e quebrava por falta do Visual Studio Build Tools)
pandas
numpy
scipy
pyarrow

# Acesso aos dados no S3 (camada Silver/Gold da Fase 2)
boto3

# Machine Learning (pipeline, modelos, busca de hiperparâmetro)
scikit-learn
joblib

# Interpretabilidade de modelos
shap

# Visualização
matplotlib
seaborn

# Notebooks
jupyter
ipykernel

# Utilitários
python-dotenv
