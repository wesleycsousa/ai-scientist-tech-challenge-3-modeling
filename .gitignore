# Ambiente virtual
.venv/
venv/
env/

# Credenciais e segredos - NUNCA versionar
.env
.env.*
*.pem
*.key
credentials
credentials.json
.aws/

# Material de apoio pessoal (enunciado em PDF, anotações, roteiro e slides da
# apresentação) - não faz parte do entregável técnico
doc/
notas/
apresentacao/
reports/referencias/*
!reports/referencias/selecao_features_revisado.csv
reports/plano-modelagem-selecao-features.md

# Dados (a base é pesada e vem do S3, não faz sentido versionar no Git)
data/raw/*
data/processed/*
!data/raw/.gitkeep
!data/processed/.gitkeep

# Jupyter
.ipynb_checkpoints/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Modelos treinados: o modelo final (models/arvore_decisao_final.joblib) é
# pequeno (< 0,1 MB) e é versionado de propósito, pra quem clonar conseguir
# rodar src/modeling/prever.py sem retreinar

# Sistema operacional / editor
.DS_Store
Thumbs.db
.vscode/settings.json
